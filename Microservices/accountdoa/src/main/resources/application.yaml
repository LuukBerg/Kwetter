spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kwetter?serverTimezone=UTC
    password: root
    username: root
  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
      show-sql: true
server:
  port: 9092


